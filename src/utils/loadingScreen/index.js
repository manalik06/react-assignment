import React from "react"
export const Loading = (props) => {
    const style = {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
    }
    return (
        <h1 style={style}> Loading...... </h1>
    )

}